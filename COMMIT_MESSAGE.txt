fix: replace silent stub functions with proper Result-based error handling

Critical improvements to error handling in cv-3d and cv-calib3d crates:

CV-3D (3d/src/gpu/mod.rs):
- gpu::registration::icp_point_to_plane(): Option<T> → Result<T, String>
  Prevents silent None returns; directs users to cv-registration crate

- gpu::mesh::laplacian_smooth(): () → Result<(), String>
  Clear error indicating GPU kernel requirement

- gpu::mesh::compute_vertex_normals(): Vec<T> → Result<Vec<T>, String>
  Prevents ambiguous empty vector returns; essential for mesh rendering

- gpu::tsdf::integrate_depth(): () → Result<(), String>
  Clear documentation of CRITICAL missing implementation for 3D reconstruction

- gpu::raycasting::cast_rays(): Vec<Option<T>> → Result<Vec<Option<T>>, String>
  Prevents ambiguous empty returns; essential for rendering and physics

CV-Calib3D (calib3d/src/chessboard.rs):
- find_chessboard_corners_robust(): Simplified stub with comprehensive docs
  Removed misleading skeleton code; clear explanation of grid assembly blocker
  Recommends Harris-based find_chessboard_corners() alternative

All functions now include:
- Comprehensive documentation explaining what's missing and why
- Clear error messages for callers
- Implementation guidance for future developers
- References to working alternatives where available
- Performance and algorithm notes

BREAKING CHANGES:
- Return types changed from Option<T>/Vec<T>/() to Result<T, String>
- Callers must update pattern matching
- Migration: option.map() → result.ok(); if empty → if err

This change prioritizes developer experience and reliability:
- Silent failures → clear errors
- Ambiguous empty values → explicit errors
- Dead code → documented stubs with guidance
