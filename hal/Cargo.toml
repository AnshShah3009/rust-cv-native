[package]
name = "cv-hal"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Hardware Abstraction Layer for multi-backend acceleration"

[dependencies]
cv-core = { path = "../core" }
rayon = { workspace = true }
nalgebra = { workspace = true }

thiserror = "2.0"
wgpu.workspace = true
pollster = "0.4.0"
futures.workspace = true
bytemuck = { version = "1.25.0", features = ["derive"] }

[features]
default = []
cpu = []
simd = []
cuda = []
tensorrt = []
directml = []
webgpu = []
